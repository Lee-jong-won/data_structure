#include "threadedTree.h"

threadTree::iterator threadTree::find_node(int search_data)
{
    // data를 tree내에서 찾아 해당 데이터를 저장한 노드 iterator 반환
}

threadTree::iterator threadTree::begin()
{
    // head 노드만 존재할 때, 예외처리. 
    // head 노드외에 다른 노드가 추가 되어 있을 때, 정상적으로
    // inorder 탐색 시 첫번째 노드를 가리키는 iterator 반환
}

